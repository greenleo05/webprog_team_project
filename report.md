# 웹 프로그래밍 팀 프로젝트 결과 보고서

## 표지
* **과제명**: 웹 프로그래밍 팀 프로젝트 기말 과제 (컴공생 마법소녀의 하루)
* **팀명**: [팀명을 입력하세요]
* **팀원 (이름/학번)**: 
  * [팀원1 이름] / [학번]
  * [팀원2 이름] / [학번]
  * [팀원3 이름] / [학번]
  * [팀원4 이름] / [학번]
* **제출일**: 2026년 6월 [O]일

---

## 1. 게임 시나리오 및 동작 방법 (매뉴얼)

### 1.1 게임 시나리오
**"컴공생 마법소녀의 하루"**
플레이어는 컴퓨터공학과 학생이자 마법소녀가 되어, 하루 동안 벌어지는 다양한 일상 속 "버그"와 시련을 물리치고 무사히 과제를 제출해야 했습니다. 
총 5가지의 스테이지(교시)로 게임을 구성했습니다.
* **1스테이지(지하철)**: 붐비는 출근길 인파 속에서 꿋꿋하게 버티는 길막러 돌파
* **2스테이지(강의실)**: 조는 학생들을 깨우고 교수님의 질문 폭격 방어
* **3스테이지(학식)**: 밥을 먹으며 에너지를 보충하는 보너스 스테이지
* **4스테이지(조별과제)**: 연락이 안 되거나 무임승차하는 트롤 조원 처리
* **5스테이지(보스전)**: 세미콜론(;) 모양의 괴물(버그) 보스를 처치하고 무사히 과제 제출

### 1.2 게임 동작 방법 (매뉴얼)
* **이동**: 좌/우 화살표 키(`←`, `→`)를 사용하여 패들(받침대)을 움직일 수 있게 만들었습니다.
* **조작**: 튕겨진 공을 받아쳐 화면 상단의 블록이나 적을 타격하도록 했습니다. 
* **궁극기**: 게임을 진행하며 오른쪽 필살기 게이지를 꽉 채운 뒤 **스페이스바**를 누르면, 다수의 유성이 적을 공격하는 화려한 빔 필살기가 발동되도록 구현했습니다.
* **아이템**: 보스가 떨어뜨리는 낙하물 중 'A+'를 획득하면 공이 늘어나 화력이 세지고, 'F'를 맞으면 패들의 길이가 일시적으로 짧아지는 패널티를 주었습니다.
* **일시정지**: `ESC` 또는 `P` 키를 눌러 언제든 게임을 멈출 수 있게 만들었습니다.

---

## 2. 핵심 기능 및 기능 구현을 위한 기술적 상세 명세

### 2.1 보스전 기믹 (5스테이지)
* **블랙홀 기능**: 보스의 체력이 절반 이하로 떨어지면 맵 중앙에 블랙홀이 나타나게 했습니다. 코드에서는 `balls` 배열에 있는 공들과 블랙홀 사이의 거리를 계산해서, 일정 거리 안으로 들어오면 공의 이동 속도(`dx`, `dy`)에 끌어당기는 방향의 수치를 더해주어 공이 휘어지도록 구현했습니다.
* **벽돌 설치 (방해물 생성)**: 보스가 임의의 좌표에 큰따옴표(`""`) 모양의 단단한 블록을 설치하도록 만들었습니다. 단순히 배경에 그리는 것이 아니라, 실제 벽돌 객체와 동일하게 만들어서 공이 닿았을 때 부딪혀서 튕겨나가도록 물리적인 처리를 넣었습니다.
* **보스가 던지는 낙하물 (A+, F)**: 보스가 일정한 타이머 간격으로 아이템과 함정을 아래로 떨어뜨리게 작성했습니다.
  * `A+`: 패들과 부딪히면 기존 공의 방향(`dy`)을 반대로 바꿔서 새로운 공 객체를 `balls` 배열에 추가했습니다. (멀티볼 기능)
  * `F`: 맞으면 단순히 체력을 깎는 대신, 자바스크립트의 `setTimeout` 함수를 써서 5초 동안 패들의 가로 길이를 반으로 줄어들게 만들었습니다.

### 2.2 게임 내 핵심기능
* **게이지(궁극기)**: 벽돌을 부술 때마다 `ultimateGauge` 변수 값을 1씩 늘렸습니다. 게이지가 꽉 차고 스페이스바를 누르면 필살기가 나가게 되는데, 이때 반투명한 마법 빔 이미지의 투명도(`alpha`)를 조금씩 높여가며 화면에 그리게 해서 서서히 밝아지는 컷신 연출을 주었습니다.
* **일시정지 기능**: ESC 키를 누르면 `isPaused` 변수를 반대로 바꿔서 캔버스에 화면을 그리는 `draw()` 함수의 실행을 멈추게 했습니다. 이때 일시정지를 너무 빨리 반복하면 화면을 그리는 함수가 중복으로 겹쳐 게임 속도가 엄청나게 빨라지는 문제가 발생했는데, 이를 막기 위해 새 프레임을 부르기 직전에 이전의 애니메이션 일정을 강제로 취소시키는 안전 코드를 추가해 해결했습니다.
* **도움말 기능**: HTML과 CSS의 `z-index`를 겹쳐서 게임 화면보다 가장 위쪽에 팝업 창 모양의 요소가 뜨도록 코드를 작성했습니다. 이를 통해 게임을 가리지 않으면서 조작법을 볼 수 있게 했습니다.
* **처음부터 / 이어하기 기능**: 각 스테이지를 클리어할 때마다 브라우저가 제공하는 `localStorage` 기능을 사용해 도달한 최고 스테이지 번호를 저장했습니다. 게임을 껐다 다시 켰을 때 이 번호를 읽어와서 '이어하기' 버튼을 누르면 해당 교시의 인덱스로 바로 이동하게 만들었습니다.
* **개발자(시연) 모드 진입 기능**: 플레이 테스트를 쉽게 하기 위해 메인 화면의 제목을 1초 안에 3번 클릭하는 조건을 코드로 짜서 숨겨진 메뉴를 띄웠습니다. 이 모드가 켜지면 `isDevMode` 변수가 참(true)이 되어서 목숨이 아주 많아지고 전체 스테이지를 고를 수 있게 했습니다.
* **스테이지 중간 컷신 기능**: 다음 교시로 넘어갈 때 5초간 기다리도록 `setTimeout` 함수를 썼습니다. 그동안 각 교시에 맞는 배경화면 이미지와 재미있는 문구를 캔버스 위에 띄워준 다음, 시간이 끝나면 본 게임 화면을 불러오게 연출했습니다.
* **스코어 시간 보너스 기능 및 랭크 시스템**: 블록을 부숴 얻은 점수에 남은 시간(`timeLeft`)을 곱한 보너스 수치를 더해 최종 스코어를 구했습니다. 이 스코어가 특정 점수 구간에 들어가는지 `if~else` 문으로 범위를 검사해서, 대학교 성적처럼 A+부터 F 학점까지 결과가 나오게 했습니다.

### 2.3 스테이지별 기능
* **여러 번 깨야 부서지는 벽돌 (1, 2스테이지)**: 단번에 부서지는 벽돌과 달리, 벽돌 정보 안에 체력(`hp`) 값을 따로 주었습니다. 공이 닿았을 때 벽돌을 바로 배열에서 지우지 않고 체력을 1씩 빼면서, 그 체력 값의 변화에 맞춰 벽돌을 칠하는 색상(`fillStyle`)이 동적으로 바뀌도록 캔버스 코드를 작성했습니다.
* **흔들리면서 내려오는 벽돌 (4스테이지 조별과제)**: 조별과제의 '트롤 조원' 벽돌들은 제자리에 고정되지 않고 매번 화면이 새로 그려질 때마다 아래로(`dropY` 값을 더해서) 떨어지게 만들었습니다. X축 좌표에는 삼각함수인 `Math.sin()`을 더해 시간이 지날수록 벽돌이 좌우로 불안하게 흔들리면서 내려오는 것처럼 보이게 구현했습니다. 부수지 못해 바닥에 닿으면 50점이 깎이게 설정했습니다.

---

## 3. 기여도 및 주요 구현 사항 상세 기술
*(※ 전체 팀원 합의 하에 실제 기여도 백분율을 입력하고, 세부 역할 내용도 수정해 주세요)*

| 팀원 이름 | 기여도 (%) | 주요 구현 사항 상세 기술 |
|---|---|---|
| **[이름 입력]** | **00%** | - 게임의 기본 뼈대가 되는 `requestAnimationFrame` 캔버스 화면 그리기 로직 완성<br>- 공과 벽돌이 닿았는지 거리를 측정해 부딪히면 반대로 밀어내는 충돌 코드 구현<br>- 패들 어디에 맞느냐에 따라 날아가는 각도가 바뀌는 코드 작성<br>- 여러 개의 공(`balls` 배열)이 동시에 튀어 다니는 멀티볼 구현 |
| **[이름 입력]** | **00%** | - 5스테이지 보스 체력과 부드럽게 움직이는 패턴(`Math.sin`, `Math.cos`) 구현<br>- 장애물 벽돌과 아이템(A+, F)을 떨어뜨리고 시간을 체크하는 타이머 연동<br>- 게이지 점수 시스템 및 스페이스바 입력 시 궁극기 효과 연출<br>- 브라우저의 저장소(`localStorage`)를 활용한 이어하기 시스템 및 속도 버그 수정 |
| **[이름 입력]** | **00%** | - 0~3스테이지의 맵 디자인 배치 및 음식, 사람 모양 커스텀 블록 그리기 구현<br>- 4스테이지 '트롤 벽돌'이 `Math.sin()`으로 흔들리며 떨어지게 만드는 코드 작성<br>- 블록이 깨졌을 때 파편 객체 배열을 만들어 사방으로 흩어지게 하는 파티클 구현<br>- 남은 시간에 비례한 점수 및 대학생 컨셉 랭크(A+~F) 판별 조건문 작성 |
| **[이름 입력]** | **00%** | - `index.html`, `style.css`를 활용해 팝업창(모달) 등 전반적인 UI 레이아웃 구현<br>- `setTimeout`을 이용해 5초간 대기하며 문구가 나오는 스테이지 사이의 컷신 구현<br>- 자바스크립트로 이미지의 픽셀 정보를 읽어서 흰색 배경만 투명하게 지우는 코드 작성<br>- 메인 화면 제목을 3번 누르면 치트키 창이 나오는 개발자 모드 로직 구현 |
