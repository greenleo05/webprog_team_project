# 5스테이지 보스전 패턴 및 다중 공(Multi-ball) 시스템 구현 계획

최종 과제 스테이지의 보스전(Phase 3)을 다채롭게 만들기 위해, 아이템 드랍을 통한 공 분열 기능과 보스의 방해 패턴(콩알탄 발사)을 추가합니다. 

## User Review Required

> [!IMPORTANT]
> 다중 공 시스템은 기존 `ball` 단일 객체를 배열(`balls = []`) 구조로 전면 개편해야 하므로 충돌 판정 및 낙하 로직에 상당한 변화가 발생합니다. 아래의 **추가 아이디어**를 포함해 구현할지 의견을 부탁드립니다.

## Open Questions

> [!TIP]
> **추가 아이디어 제안 (검토 요망)**
> 제가 보스전의 재미를 위해 추가로 두 가지 패턴을 고안해 보았습니다.
> 1. **패들 축소 (Memory Leak)**: 보스가 투사체를 쏘고 패들이 이것에 맞으면 패들 크기가 일정 시간 동안 절반으로 줄어듭니다.
> 2. **블랙홀 (Infinite Loop)**: 보스 체력이 50% 이하가 되면 캔버스 특정 영역에 중력장(블랙홀)이 생겨, 그 근처를 지나는 공들의 궤도를 비틀어 버립니다.
> 
> 제안해주신 1, 2번 패턴(공 분열 아이템, 공 제거 콩알탄)과 함께 위 두 패턴도 구현하는 것이 좋을지, 아니면 일단 1번과 2번 기능에만 집중할지 피드백 부탁드립니다.

## Proposed Changes

### 1. 다중 공(Multi-ball) 시스템 기반 마련
#### [MODIFY] `game.js`
- 기존 단일 `ball` 객체를 폐기하고 `balls = []` 배열 체제로 전환합니다.
- `draw()`, `collisionDetection()` 내의 공 물리 연산, 외벽 튕김, 패들 반사 등을 `balls.forEach(b => { ... })` 형태로 리팩토링합니다.
- 공이 화면 아래로 떨어질 경우 
  - `balls.length > 1` 이면 해당 공만 배열에서 제거.
  - `balls.length === 1` 이면 기존처럼 라이프(목숨) 차감 및 재생성(Respawn) 로직 실행.

### 2. 아이템 및 투사체 렌더링 및 충돌 시스템
#### [MODIFY] `game.js`
- `items = []`, `projectiles = []` 전역 배열을 선언하여 상태를 관리합니다.
- **아이템 드랍 (보스 패턴 1)**: 보스전 돌입 시 일정 주기(예: 8~10초)마다 보스 위치에서 하단 방향으로 보조 아이템이 떨어집니다. 패들과 충돌 시 획득되며, 현재 활성화된 첫 번째 공의 위치에서 다른 반사각을 가진 새로운 공 객체를 `balls.push()`로 추가합니다.
- **콩알탄 투사체 (보스 패턴 2)**: 매 3~4초마다 보스가 붉은 콩알탄(Syntax Error 텍스트 등)을 랜덤 각도 또는 공의 현재 위치를 향해 발사합니다.
- **투사체-공 충돌 로직**: 콩알탄과 캔버스 내의 공(`balls` 요소) 간의 거리(유클리드 거리)를 측정하여 닿았을 시:
  - 콩알탄은 사라집니다.
  - 다중 공 상태라면 닿은 공 하나만 소멸시킵니다.
  - 하나 남은 공이라면 라이프를 깎고 리스폰 처리합니다.

## Verification Plan
### Manual Verification
- **분열 동작**: 보스전에서 아이템 획득 시 2개 이상의 공이 생기고 각자 물리 반사 및 벽돌/보스 타격을 정상적으로 수행하는지 확인합니다.
- **소멸/게임오버 동작**: 공이 콩알탄에 맞았을 때, 공이 여러 개일 때는 1개만 소멸하고 마지막 1개일 때 맞으면 생명력이 줄어들고 화면이 플리커링(리스폰)되는지 검증합니다.
- **성능 안정성**: 공 개수나 투사체 개수가 많아져도 메모리 누수 없이 60fps가 유지되는지 확인합니다.
